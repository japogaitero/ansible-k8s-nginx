[defaults]
# Esta línea es la clave para que no tengas que usar -i ni variables
inventory = ./hosts
interpreter_python = ./venv/bin/python

[privilege_escalation]
become = False


# [defaults]
# # Indica a Ansible que use el Python del entorno virtual de esta carpeta
# # interpreter_python = ./venv/bin/python
# interpreter_python = /home/victor/github-projects/ansible-k8s-ghost/venv
# # Opcional: Evita avisos sobre hosts desconocidos si usas SSH en el futuro
# host_key_checking = False
